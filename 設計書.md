# Colosseum 技術設計書

作成日: 2026-09-17

---

## 1. システムアーキテクチャ全体図

```
┌─────────────────────────────────────────────────────────┐
│                    ブラウザ（クライアント）               │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  InputManager │  │  Renderer    │  │  StateStore  │  │
│  │  (キー入力)   │  │  (Canvas描画)│  │  (ゲーム状態)│  │
│  └──────┬───────┘  └──────▲───────┘  └──────▲───────┘  │
│         │                 │                 │           │
│         └────────► SocketClient ◄───────────┘           │
└──────────────────────────┬──────────────────────────────┘
                           │ WebSocket
┌──────────────────────────▼──────────────────────────────┐
│                    Node.js サーバー                      │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  RoomManager │  │  GameLoop    │  │  BotManager  │  │
│  │  (ルーム管理)│  │  (20 TPS)    │  │  (BOT制御)   │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                 │           │
│         └─────────────────▼─────────────────┘           │
│                     GameRoom                            │
│              (Entity管理・物理・当たり判定)              │
│                           │                             │
│                    ┌──────▼──────┐                      │
│                    │    Redis    │                       │
│                    │(ルーム情報) │                       │
│                    └─────────────┘                      │
└─────────────────────────────────────────────────────────┘
```

---

## 2. ディレクトリ構造

```
colosseum/
├── server/
│   ├── index.js              # エントリーポイント、Socket.io 初期化
│   ├── RoomManager.js        # ルームの作成・削除・一覧管理
│   ├── GameRoom.js           # ルームごとのゲームループ・エンティティ管理
│   ├── entities/
│   │   ├── Player.js         # プレイヤーエンティティ
│   │   ├── Enemy.js          # 敵エンティティ基底クラス
│   │   ├── enemies/
│   │   │   ├── BasicEnemy.js
│   │   │   ├── FastEnemy.js
│   │   │   ├── RangedEnemy.js
│   │   │   └── BossEnemy.js
│   │   └── Bullet.js         # 弾丸エンティティ
│   ├── systems/
│   │   ├── SpawnSystem.js    # 敵スポーン計算
│   │   ├── CollisionSystem.js# 当たり判定
│   │   ├── AISystem.js       # 敵AI
│   │   └── ScoreSystem.js    # スコア計算
│   ├── BotPlayer.js          # BOT プレイヤー
│   └── config.js             # ゲームパラメータ定数
│
├── client/
│   ├── index.html
│   ├── js/
│   │   ├── main.js           # エントリーポイント、画面遷移
│   │   ├── SocketClient.js   # WebSocket 通信
│   │   ├── InputManager.js   # キー入力管理
│   │   ├── StateStore.js     # ゲーム状態保持
│   │   ├── Renderer.js       # Canvas 描画
│   │   └── screens/
│   │       ├── HomeScreen.js # ホーム画面
│   │       └── GameScreen.js # ゲーム画面
│   └── css/
│       └── style.css
│
└── shared/
    └── constants.js          # サーバー・クライアント共通定数
```

---

## 3. サーバー設計

### 3-1. GameRoom クラス

```
GameRoom {
  id: string                  # ルームID (UUID v4 の先頭8文字)
  players: Map<socketId, Player>
  enemies: Map<id, Enemy>
  bullets: Map<id, Bullet>
  state: 'active' | 'help' | 'clear' | 'ended'
  startTime: number           # Date.now()
  elapsedSec: number          # 経過秒数（ティックごと更新）
  helpCooldown: number        # ヘルプ叫ぶクールダウン残り秒
  bossSpawned: boolean
  tickInterval: NodeJS.Interval

  tick()                      # 20 TPS で呼ばれるメインループ
  addPlayer(socket, isBot)
  removePlayer(socketId)
  serialize(): GameStatePayload  # 通信用シリアライズ
  destroy()
}
```

### 3-2. ゲームループ（tick）処理順序

```
1. elapsedSec 更新
2. SpawnSystem.update(room)    # スポーン判定・敵生成
3. AISystem.update(room)       # 敵の移動・攻撃意思決定
4. BotManager.update(room)     # BOT の入力生成
5. 各 Player の入力適用 → 座標更新
6. 各 Enemy の移動 → 座標更新
7. 各 Bullet の移動 → 座標更新
8. CollisionSystem.update(room)# 当たり判定 → ダメージ適用
9. 死亡エンティティの削除
10. ボス出現チェック（elapsedSec >= BOSS_SPAWN_SEC）
11. ルーム終了チェック（全プレイヤー死亡 or ボス討伐）
12. serialize() → 接続中全クライアントに game_state 送信
```

### 3-3. SpawnSystem

```javascript
// server/systems/SpawnSystem.js

const BASE_INTERVAL = 8.0      // 秒
const GROWTH_RATE   = 0.03
const SPEED_FACTOR  = { 1: 1.0, 2: 1.5, 3: 2.2, 4: 3.0 }
const BOT_JOIN_DELAY = 30      // ヘルプから30秒後にBOT参加

update(room) {
  const n = clamp(room.players.size, 1, 4)
  const sf = getSpeedFactor(room, n)  // 遅延考慮（後述）
  const interval = BASE_INTERVAL / (sf * (1 + GROWTH_RATE * room.elapsedSec))

  room.spawnAccum += TICK_DELTA  // 1/20 秒
  if (room.spawnAccum >= interval) {
    room.spawnAccum = 0
    spawnEnemy(room)
  }

  // ボス出現予告（30秒前）
  if (!room.bossWarned && room.elapsedSec >= BOSS_SPAWN_SEC - 30) {
    emit(room, 'boss_warning')
    room.bossWarned = true
  }
  // ボス出現
  if (!room.bossSpawned && room.elapsedSec >= BOSS_SPAWN_SEC) {
    spawnBoss(room)
    room.bossSpawned = true
  }
}

// 助っ人参加から15秒間は speed_factor を据え置く
getSpeedFactor(room, n) {
  const sinceJoin = Date.now() - room.lastJoinTime
  if (sinceJoin < 15_000) return SPEED_FACTOR[max(n - 1, 1)]
  return SPEED_FACTOR[n]
}
```

### 3-4. CollisionSystem

```
update(room) {
  // 敵 → プレイヤー 衝突
  for each enemy in room.enemies:
    for each player in room.players:
      if circle_collide(enemy, player) and enemy.attackCooldown <= 0:
        player.hp -= enemy.attackPower
        enemy.attackCooldown = enemy.attackRate
        emit(room, 'damage_event', { targetId: player.id, damage, hp: player.hp })
        if player.hp <= 0: killPlayer(room, player)

  // プレイヤー攻撃 → 敵 衝突
  for each player in room.players (with attackActive flag):
    for each enemy in room.enemies:
      if circle_collide(player.attackCircle, enemy):
        enemy.hp -= player.attackPower
        if enemy.hp <= 0: killEnemy(room, enemy, player)

  // 弾丸 → プレイヤー 衝突
  for each bullet in room.bullets:
    for each player in room.players:
      if circle_collide(bullet, player):
        player.hp -= bullet.damage
        destroyBullet(room, bullet)
}

circle_collide(a, b) {
  return distance(a.pos, b.pos) < (a.radius + b.radius)
}
```

---

## 4. エンティティ設計

### 4-1. Player

```javascript
{
  id: string,           // socket.id
  name: string,
  pos: { x, y },
  vel: { x, y },        // 入力から計算
  hp: number,
  maxHp: number,        // 100 or 150（助っ人ボーナス）
  attackPower: number,  // 20 or 30（助っ人ボーナス）
  radius: 16,
  attackCooldown: number,
  invincibleTimer: number,  // 被弾後無敵時間
  scoreMultiplier: number,  // 1.0 or 1.5〜2.0（助っ人ボーナス）
  isBot: boolean,
  isDead: boolean,
  input: { dx, dy, attacking }  // クライアントから受け取る
}
```

### 4-2. Enemy 基底

```javascript
{
  id: string,           // 連番
  type: 'basic' | 'fast' | 'ranged' | 'boss',
  pos: { x, y },
  vel: { x, y },
  hp: number,
  maxHp: number,
  speed: number,
  attackPower: number,
  attackRange: number,
  attackRate: number,   // 秒
  attackCooldown: number,
  radius: number,
  targetId: string | null  // 追尾対象プレイヤーID
}
```

### 4-3. 敵パラメータ定数（server/config.js）

```javascript
ENEMY_CONFIG = {
  basic:   { hp: 30,   speed: 80,  attackPower: 10, attackRange: 30,  attackRate: 1.0, radius: 12 },
  fast:    { hp: 15,   speed: 160, attackPower:  8, attackRange: 25,  attackRate: 0.8, radius:  8 },
  ranged:  { hp: 40,   speed: 50,  attackPower: 15, attackRange: 350, attackRate: 2.0, radius: 14 },
  boss:    { hp: 2000, speed: 40,  attackPower: 40, attackRange: 60,  attackRate: 2.5, radius: 40 },
}
```

---

## 5. クライアント設計

### 5-1. StateStore

```javascript
// クライアント側のゲーム状態（サーバーから受け取った最新状態）
StateStore = {
  myPlayerId: string,
  players: Map<id, PlayerState>,
  enemies: Map<id, EnemyState>,
  bullets: Map<id, BulletState>,
  elapsedSec: number,
  enemyCount: number,
  roomState: 'active' | 'help' | 'clear' | 'ended',

  // サーバーから game_state を受け取るたびに上書き
  update(payload: GameStatePayload): void,

  // 描画用（前フレームとの線形補間）
  interpolate(alpha: number): InterpolatedState,
}
```

### 5-2. Renderer

```javascript
// 60FPS でrequestAnimationFrame から呼ばれる
Renderer.draw(interpolatedState) {
  ctx.clearRect(...)

  // カメラ変換（自プレイヤー中心）
  ctx.save()
  ctx.translate(canvas.width/2 - myPlayer.pos.x, canvas.height/2 - myPlayer.pos.y)

  drawMap()           // グリッド背景
  drawEnemies()       // 赤い円など
  drawBullets()       // 小さな円
  drawPlayers()       // 色付き円 + 名前 + HPバー
  drawBoss()          // 黒い大円（出現時）

  ctx.restore()
  drawHUD()           // カメラ変換外（固定UI）
}
```

### 5-3. 描画仕様（MVP 幾何学）

| エンティティ | 形状 | 色 |
|---|---|---|
| 自プレイヤー | 円 r=16 | 青 |
| 他プレイヤー | 円 r=16 | 緑 |
| BOT | 円 r=16 | 水色（破線枠） |
| 攻撃範囲 | 円（点滅） | 青半透明 |
| 雑魚敵 | 円 r=12 | 赤 |
| 高速敵 | 円 r=8 | オレンジ |
| 遠距離敵 | 四角 14×14 | 紫 |
| ボス | 円 r=40 | 黒（赤枠） |
| 弾丸 | 円 r=4 | 紫 |
| マップ背景 | グリッド | 暗いグレー |

### 5-4. InputManager

```javascript
// キー状態を毎フレーム収集 → 20ms ごとにサーバーへ送信
InputManager = {
  keys: Set<string>,

  getInput(): InputPayload {
    const dx = (keys.has('d') || keys.has('ArrowRight')) - (keys.has('a') || keys.has('ArrowLeft'))
    const dy = (keys.has('s') || keys.has('ArrowDown'))  - (keys.has('w') || keys.has('ArrowUp'))
    const attacking = keys.has(' ')
    return { dx, dy, attacking }
  }
}

// 送信レート: 50ms ごと（サーバー 20TPS に合わせる）
setInterval(() => socket.emit('player_input', InputManager.getInput()), 50)
```

---

## 6. WebSocket 通信プロトコル詳細

### 6-1. C→S イベント

```typescript
// player_input（50ms ごと）
{ dx: -1|0|1, dy: -1|0|1, attacking: boolean }

// help_call（クールダウン 60s）
{}

// room_create
{ playerName: string }

// room_join
{ roomId: string, playerName: string }
```

### 6-2. S→C イベント

```typescript
// game_state（20 TPS = 50ms ごと）
{
  seq: number,          // シーケンス番号（パケットロス検知）
  t: number,            // サーバー時刻
  elapsedSec: number,
  players: [{
    id, name, x, y, hp, maxHp, isDead, isBot, scoreMultiplier
  }],
  enemies: [{
    id, type, x, y, hp, maxHp
  }],
  bullets: [{
    id, x, y
  }]
}

// damage_event
{ targetId: string, damage: number, remainHp: number }

// player_join
{ playerId: string, name: string, isBot: boolean, bonus: { hp, atk, scoreMultiplier } }

// player_die
{ playerId: string }

// enemy_spawn
{ id: string, type: string, x: number, y: number }

// boss_warning  （出現30秒前）
{}

// boss_spawn
{ x: number, y: number }

// help_activated
{ roomId: string }   # ルーム一覧更新用

// room_clear
{ elapsedSec, kills, score, title: string | null }

// room_end
{ reason: 'all_dead' }

// room_list（ホーム画面向け、2秒ごと or 変化時）
[{ id, playerCount, enemyCount, elapsedSec, isHelp }]
```

---

## 7. Redis データ設計

```
# ルーム情報（TTL: 30分）
Key: room:{roomId}
Type: Hash
Fields:
  state         active | help | clear | ended
  playerCount   number
  enemyCount    number
  elapsedSec    number
  isHelp        0 | 1
  startTime     timestamp

# ルーム一覧（ホーム画面表示用インデックス）
Key: rooms:active
Type: Set
Value: roomId の集合

# プレイヤー → ルーム のマッピング（切断時のルーム特定用）
Key: player:{socketId}:room
Type: String
Value: roomId
TTL: 1時間
```

---

## 8. BOT AI 設計

```javascript
// BotPlayer.js
// 毎ティック update() が呼ばれ、input を生成する

BotPlayer.update(room) {
  const target = findNearestEnemy(this.pos, room.enemies)
  if (!target) {
    this.input = { dx: 0, dy: 0, attacking: false }
    return
  }

  const dist = distance(this.pos, target.pos)
  const dir  = normalize(subtract(target.pos, this.pos))

  if (dist > ATTACK_RANGE) {
    // 近づく
    this.input = { dx: dir.x, dy: dir.y, attacking: false }
  } else {
    // 攻撃
    this.input = { dx: 0, dy: 0, attacking: true }
  }
}
```

BOT の限界設計（意図的）:
- 複数の敵に囲まれると対処できない（単純な最近接追尾のみ）
- ボスには近接しかできない → ソロクリアが極高難度になる理由

---

## 9. スコア計算

```javascript
// server/systems/ScoreSystem.js

calculateFinalScore(player, room) {
  const survival  = room.elapsedSec * 10
  const kills     = player.killCount * 50
  const clearBonus = room.state === 'clear' ? 10_000 : 0
  const raw = survival + kills + clearBonus
  return Math.floor(raw * player.scoreMultiplier)
}

// 助っ人スコア倍率（参加時の経過時間に比例）
calcScoreMultiplier(elapsedSecAtJoin) {
  // 0分参加 → 1.0x、5分以上参加 → 2.0x
  return clamp(1.0 + (elapsedSecAtJoin / 300), 1.0, 2.0)
}
```

---

## 10. 当たり判定最適化

エンティティ数が多い（〜500体）場合、O(n²) の総当たりはCPU負荷大。

### 空間分割（グリッド法）

```
マップ 2048×2048 を 64px グリッドに分割 → 32×32 = 1024 セル

毎ティック:
1. 全エンティティをグリッドに登録（O(n)）
2. 当たり判定は同グリッド + 隣接グリッドのみ確認（O(n) 平均）

実装:
SpatialGrid = {
  cells: Map<cellKey, Set<Entity>>,
  cellSize: 64,

  insert(entity)   { cells[key(entity.pos)].add(entity) }
  query(pos, r)    { return entities in cells within radius r of pos }
  clear()          { cells.clear() }
}
```

MVP では実装しない（500体程度なら総当たりでも許容範囲）。  
500体超えたらスポーン抑制 → 実質不要。正式版で検討。

---

## 11. 開発順序

```
Phase 1: サーバー基盤
  ├── Socket.io セットアップ
  ├── RoomManager（作成・参加・削除）
  ├── GameRoom（ティックループ）
  └── Player 移動同期（敵なし）

Phase 2: 敵システム
  ├── SpawnSystem（雑魚のみ）
  ├── AISystem（直進追尾）
  └── CollisionSystem（敵→プレイヤー）

Phase 3: 戦闘
  ├── プレイヤー攻撃（当たり判定）
  ├── 敵 HP・死亡
  └── プレイヤー HP・死亡・ルーム消滅

Phase 4: 助っ人システム
  ├── ルーム一覧（ホーム画面）
  ├── ヘルプ叫ぶ
  ├── 助っ人ボーナス
  └── BOT参加

Phase 5: ボス・クリア
  ├── ボス出現・AI
  └── クリア判定・スコア

Phase 6: UI 整備
  ├── HUD
  ├── クリア / ゲームオーバー画面
  └── バランス調整
```
