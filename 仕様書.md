# Colosseum ゲーム仕様書

作成日: 2026-09-17  
企画書決定事項を反映済み

---

## 1. ゲーム概要

- ジャンル: 多人数参加型ブラウザ生存ゲーム
- 視点: 2D トップビュー
- コアループ: ルーム作成 → 敵が指数的に増殖 → 助っ人を呼ぶ → ボス討伐
- 終了条件（失敗）: ルーム内プレイヤー数がゼロ → ルーム消滅
- 終了条件（成功）: ボス討伐 → ルームクリア + 高スコア

---

## 2. 技術スタック

| レイヤー | 採用 | 備考 |
|---|---|---|
| 描画 | HTML5 Canvas | ゲームエンジン不使用。幾何学シェイプのみ（MVP） |
| リアルタイム通信 | WebSocket（Socket.io） | |
| サーバー | Node.js | ゲームループ・当たり判定はサーバー権威 |
| 状態管理 | サーバー権威モデル | クライアントは描画専用 |
| DB | Redis | ルーム・セッション情報（揮発でよい） |
| ゲームループ | 20 TPS（サーバー）/ 60 FPS（クライアント） | 通信量とレスポンスのバランス |

---

## 3. ホーム画面

### 画面構成

```
┌─────────────────────────────┐
│        COLOSSEUM            │
│                             │
│  プレイヤー名: [________]   │
│                             │
│   [① ルームを作る]          │
│   [② 助けに行く]            │
│                             │
│  ── アクティブルーム ──     │
│  🔴 ヘルプ中！ Room#4823    │
│     2人・敵124体・12分経過  │
│  🟡 Room#3901 1人・敵32体   │
└─────────────────────────────┘
```

### 仕様
- プレイヤー名はゲスト名（アカウント不要）。ブラウザ localStorage に保存
- ルーム一覧はリアルタイム更新（WebSocket）
- ヘルプ中フラグのルームは最上部に表示・赤色強調
- ルーム情報表示項目: ヘルプフラグ / ルームID / 人数 / 敵の数 / 経過時間

---

## 4. ルーム作成（① ルームを作る）

### フロー

1. ボタン押下 → サーバーがルーム ID 生成、プレイヤー入室
2. 2D マップに自キャラクター配置（マップ中央）
3. ゲーム開始タイマー起動、敵スポーン開始
4. ルーム URL を共有できる（コピーボタン）

### マップ仕様

| 項目 | 仕様 |
|---|---|
| サイズ | 2048 × 2048 px（スクロールあり） |
| カメラ | 自キャラクター追従 |
| 障害物 | なし（MVP。正式版で追加検討） |
| 敵スポーン位置 | マップ端ランダム |
| プレイヤー初期位置 | マップ中央 |

---

## 5. 助っ人システム（② 助けに行く）

### 参加フロー

1. ホームの「助けに行く」またはルーム一覧のルームをクリック
2. 入室 → **助っ人強化状態**でスポーン
3. スポーン位置: マップ端ランダム（ホストと同位置にならないよう分散）

### 助っ人強化ボーナス（遅れて来たヒーロー）

| パラメータ | 通常値 | 助っ人ボーナス |
|---|---|---|
| HP | 100 | 150（+50%） |
| 攻撃力 | 20 | 30（+50%） |
| スコア倍率 | 1.0x | 1.5x |

- ボーナスは参加時点の経過時間が長いほど強化（最大2倍、5分以上で上限）
- 称号「救世主」: ヘルプ中ルームに参加した場合に付与（セッション内）

### ヘルプ叫ぶ機能

- ゲーム内でプレイヤーが「ヘルプ」ボタン押下 or H キー
- ルーム一覧のヘルプフラグが ON → 赤色強調表示
- クールダウン: 60秒（連打防止）
- ヘルプ中は画面に「HELP!!!」エフェクト表示（自室内のみ）

### BOT補完

- リアル助っ人不在時、ヘルプ叫んでから **30秒後** に BOT が自動参加
- BOT の能力: 通常プレイヤーと同等（強化ボーナスなし）
- BOT の AI: 最近接の敵に向かって移動・攻撃
- リアル助っ人が参加した瞬間に BOT は退場

---

## 6. プレイヤー仕様

### 基本パラメータ

| パラメータ | 値 |
|---|---|
| HP | 100 |
| 移動速度 | 200 px/s |
| 攻撃力 | 20 |
| 攻撃範囲 | 近接: 60px 半径 |
| 攻撃クールダウン | 0.5s |
| 無敵時間（被弾後） | 0.5s |

### 操作

| 操作 | PC |
|---|---|
| 移動 | WASD / 矢印キー |
| 攻撃 | スペース（自動: 範囲内の最近接敵） |
| ヘルプ叫ぶ | H キー |

### 死亡・復活

- HP 0 → 死亡、その場で消滅
- 死亡後: 観戦モード（カメラは残存プレイヤー追従）
- 復活なし（ルーム内プレイヤー全滅でルーム消滅）

---

## 7. 敵仕様

### スポーン計算式

```
spawn_interval(t, n) = base_interval / (speed_factor(n) * (1 + growth_rate * t))

base_interval = 8.0 秒   # 最初のスポーン間隔
growth_rate   = 0.03      # 時間による加速係数（要プロトタイプ検証）
t             = 経過秒数
n             = 現在のプレイヤー数
```

### プレイヤー数による speed_factor（段階値）

| プレイヤー数 | speed_factor | 備考 |
|---|---|---|
| 1 | 1.0 | 基準 |
| 2 | 1.5 | 助っ人参加 15秒後に適用 |
| 3 | 2.2 | 〃 |
| 4+ | 3.0 | 上限 |

助っ人参加から **15秒間** は speed_factor を据え置き（安堵演出）。15秒後に段階値へ移行。

### 敵種類（MVP）

| 種類 | 形状（MVP） | 移動 | 攻撃 | HP | 出現タイミング |
|---|---|---|---|---|---|
| 雑魚 | 赤い円 | 最近接プレイヤーに直進 | 近接 10 | 30 | 開始直後〜 |
| 高速 | 橙の小円 | 高速直進 | 近接 8 | 15 | 3分〜 |
| 遠距離 | 紫の四角 | 距離保ちながら移動 | 弾丸 15 | 40 | 5分〜 |
| ボス | 黒の大円 | ゆっくり追尾 | 近接 40 / 範囲衝撃波 | 2000 | 8分〜（1体のみ） |

### 敵 AI

1. 最近接プレイヤーを追尾
2. 攻撃射程内 → 攻撃
3. プレイヤー全滅 → 停止
4. 最大同時出現数: 500体（パフォーマンス上限、超過時はスポーン抑制）

### ボス出現

- 8分経過で出現（出現30秒前に予告エフェクト + SE）
- ボス討伐 → ルームクリア画面
- ボス出現後も雑魚スポーンは継続

---

## 8. 戦闘システム

### 当たり判定

- 権威: **サーバー**（チート防止）
- プレイヤー コライダー: 半径 16px の円
- 敵コライダー: 半径（雑魚12px / 高速8px / 遠距離14px / ボス40px）
- 判定頻度: 20 TPS に同期

### ダメージ処理

```
最終ダメージ = 攻撃力
HP -= 最終ダメージ
HP が 0 以下 → 死亡
```

### 弾丸（遠距離敵）

- 速度: 300 px/s
- 射程: 400px（超過で消滅）
- サーバー上で座標計算

---

## 9. スコアシステム

### スコア計算

```
スコア = (生存時間 × 10) + (撃破数 × 50) + クリアボーナス
クリアボーナス = ボス討伐時 10000pt
助っ人倍率 = 1.0〜2.0x（参加時の経過時間に比例）
最終スコア = スコア × 助っ人倍率
```

### 称号

| 称号 | 条件 |
|---|---|
| 救世主 | ヘルプ中ルームに参加してクリア |
| ソロクリア | 1人（BOTなし）でボス討伐 |
| 生存者 | 10分以上生存 |

---

## 10. ルーム管理・リアルタイム通信

### ルームライフサイクル

```
作成 → アクティブ
     → [全員死亡] → 消滅
     → [ボス討伐] → クリア → 消滅
     → [ヘルプ中] → 助っ人参加 or BOT参加 → アクティブ継続
```

### WebSocket イベント一覧

| イベント | 方向 | 内容 |
|---|---|---|
| `player_input` | C→S | 入力方向ベクトル |
| `player_attack` | C→S | 攻撃発動 |
| `help_call` | C→S | ヘルプ叫ぶ |
| `game_state` | S→C | 全エンティティの座標・HP（20 TPS） |
| `damage_event` | S→C | ダメージ発生通知 |
| `enemy_spawn` | S→C | 敵スポーン情報 |
| `player_join` | S→C | 助っ人参加通知 |
| `player_die` | S→C | プレイヤー死亡通知 |
| `boss_spawn` | S→C | ボス出現通知 |
| `room_clear` | S→C | クリア通知 + スコア |
| `room_end` | S→C | ルーム消滅通知 |
| `room_list` | S→C | ルーム一覧更新（ホーム画面向け） |

### 状態同期

- `game_state` はフル状態送信（全エンティティ座標・HP）
- 20 TPS × エンティティ数が多い場合は差分送信に移行検討
- クライアント側で線形補間（lerp）して描画を滑らかにする

---

## 11. UI/UX

### HUD（ゲーム中）

```
┌──────────────────────────────────────────┐
│ HP: ████████░░  敵: 124体  生存: 8:32    │
│ スコア: 12,400                            │
│                                          │
│           [2D トップビュー]              │
│                                          │
│ [H: ヘルプ叫ぶ]        参加中: 3名      │
└──────────────────────────────────────────┘
```

### 表示要素

| 要素 | 表示 |
|---|---|
| 自分の HP バー | 常時表示 |
| 他プレイヤーの HP バー | キャラクター頭上に小表示 |
| 敵の HP バー | ボスのみ（画面上部に大きく） |
| 現在の敵数 | HUD 常時 |
| 生存時間 | HUD 常時 |
| スコア | HUD 常時 |
| 参加人数 | HUD 常時 |
| ヘルプクールダウン | ボタンにタイマー表示 |
| ボス出現予告 | 30秒前から画面フラッシュ + カウントダウン |

### クリア / ゲームオーバー画面

| 項目 | 内容 |
|---|---|
| 生存時間 | 表示 |
| 撃破数 | 表示 |
| 最終スコア | 表示 |
| 称号 | 該当称号を表示 |
| ボタン | 「もう一度（新ルーム）」「ホームへ」 |

---

## 12. MVP スコープ

最小実装で動くゲームとして成立する範囲:

1. ホーム画面（ルーム作成 / ルーム一覧 / 参加）
2. 単一マップ（2048×2048、障害物なし）
3. プレイヤー: WASD移動 + スペース攻撃（近接のみ）
4. 敵: 雑魚のみ（赤い円、直進追尾）
5. 指数的敵スポーン（speed_factor は人数段階値）
6. WebSocket リアルタイム同期（20 TPS）
7. ヘルプ叫ぶ機能（フラグ ON + BOT補完）
8. 助っ人強化ボーナス（HP+50%）
9. ボス（黒い大円）+ 討伐クリア
10. スコア表示 + クリア / ゲームオーバー画面

MVP 除外（正式版で追加）:
- 高速敵・遠距離敵
- 称号システム
- ドット絵グラフィック
- BGM・効果音
- スマートフォン対応

---

## 13. 要プロトタイプ検証パラメータ

実装前に数値を決め打ちせず、動かしながら調整すること:

| パラメータ | 初期値 | 検証内容 |
|---|---|---|
| `growth_rate` | 0.03 | 一人で何分持つか |
| `base_interval` | 8.0s | 序盤のペース感 |
| `speed_factor` 段階値 | 1.0 / 1.5 / 2.2 / 3.0 | 多人数時の緊張感維持 |
| 助っ人遅延 | 15s | 安堵感の長さ |
| ボス出現タイミング | 8分 | ヘルプを呼びたくなるか |
| BOT参加までの待機 | 30s | 孤独すぎないか |
| 助っ人HP倍率 | 1.5x | ヒーロー感と強すぎのバランス |
